dependencies {
    implementation(project(":newrelic-security-api"))
    implementation("com.newrelic.agent.java:newrelic-weaver-api:7.11.0-SNAPSHOT")
    implementation("commons-httpclient:commons-httpclient:3.1")
}

jar {
    manifest { attributes 'Implementation-Title': 'com.newrelic.instrumentation.security.httpclient-3.1' }
}

verifyInstrumentation {
    excludeRegex '.*-atlassian-[0-9]'

    // These are pulled from the mule repo, so we can exlcude them
    excludeRegex '.*-jbossorg-[0-9]'
    excludeRegex '.*-osgi'
    excludeRegex '.*-jenkins.*'

    passesOnly 'commons-httpclient:commons-httpclient:[3.1-rc1,3.1]'
}

site {
    title 'Apache Httpclient'
    type 'Messaging'
}